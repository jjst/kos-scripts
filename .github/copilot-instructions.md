# Copilot instructions

As part of this repository's `copilot-setup-steps` workflow, the kOS upstream docs are cloned to `/tmp/kos-docs`.

Use `/tmp/kos-docs/doc/source` as a local reference for kOS documentation when working on scripts in this repository.
